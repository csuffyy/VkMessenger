<UserControl x:Class="Messenger.Wpf.Views.EmptyHistory"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Messenger.Wpf.Views"
             mc:Ignorable="d" 
                 Background="{DynamicResource MaterialDesignPaper}"

               TextOptions.TextRenderingMode="Auto"
             TextElement.FontWeight="Regular"
    TextElement.Foreground="{DynamicResource MaterialDesignBody}"
   
             d:DesignHeight="300" d:DesignWidth="300"
                FontFamily="pack://application:,,,/MaterialDesignThemes.Wpf;component/Resources/Roboto/#Roboto">
    <Grid>
        <TextBlock Text="No recent messages ):" Style="{StaticResource MaterialDesignSubheadingTextBlock}" FontStyle="Italic" HorizontalAlignment="Center"
                   VerticalAlignment="Center"/>
    </Grid>
</UserControl>
